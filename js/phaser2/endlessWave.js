var endlessWave = {
    preload: function() {
        // load level sprites
    },
    create: function() {
        game.physics.startSystem(Phaser.Physics.ARCADE);

        // add images to tileset

        // set up layers for tileset

        
    }
}